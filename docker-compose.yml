services:
  api:
    image: storyteller_api
    build:
      context: .
      dockerfile: api.Dockerfile
    container_name: api
    environment:
      PGUSER: ${POSTGRES_USER:-postgres}
      PGPWD: ${POSTGRES_PASSWORD:-postgres} 
      DB_URL: db:5432/storyteller
    ports:
      - '8000:8000'
    networks:
      - app 
    restart: unless-stopped

  postgres:
    container_name: db
    image: postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: storyteller
    volumes:
       - storyteller_postgres:/data/postgres
    ports:
      - "5432:5432"
    networks:
      - app
    restart: unless-stopped

 
volumes:
    storyteller_postgres:
networks:
  app:
